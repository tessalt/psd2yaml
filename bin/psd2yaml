#!/usr/bin/env ruby

require 'psd2yaml'

if ARGV[0] 

  parser = Parser::Psd2yaml.new(ARGV[0], ARGV[1])

  parser.parse

else

  puts "Please specify an input .psd file"

end


# Psd2yaml.new(ARGV[0], ARGV[1]).parse 